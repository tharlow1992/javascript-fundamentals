let grades = ["A", "F", "F-", "C++", "B+"];
for (x of 'grades') {
    console.log(x);
}

// For-in-loop will return to you the VALUE, and can only run on ITERABLES (ordered collection)
//for - of: uses ITERABLE collections (array, string, etc.)
