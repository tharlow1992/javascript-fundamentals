

/*

make a tip calculator using a function
That takes in the BILL (number)
HAve it RETURN the TIP in a VARIABLE
Print that Variable

*/

/*

make function for tip
parameter = bill (number)
procedure: calculate tip (20%)
return the calculated tip

*/

let tip = (total) => total * 0.2;


let tipAmount = tip(50);

console.log(tipAmount);