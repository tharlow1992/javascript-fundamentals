
let num = 6;

// Ternary (condition) ? (true) : (false)

(num > 0)   
    ? console.log('yes') 
    : console.log('nah');

    //Exactly this below

    if (num > 0 && num < 5) {
        console.log('yes');
    } else if {
        console.log();
    }

let age = 50;

(age >= 25)
    ? console.log('Yay you can rent a car')
    : (age >= 21)
    ? console.log('Yay you can drink alcohol')
    : (age >= 18)
    ? console.log('Yay you can vote')
    : console.log('sorry bout it');
    function IAmADeclaration (cage){
        console.log(cage);
        return 'Ya boi nicolas';
        
    }
    //console log is only used to debug in the front end development process world. Sometimes important in backend.
    let generateTaxRate = (state) => 42;
       


    let location = "IN"
    let taxRate;

    
    
    
    (location === 'IN') ? taxRate = 0.05;

